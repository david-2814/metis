# AGENTS.md

Shared context for any AI agent working in this repo (Claude Code, Cursor, Codex, Aider, etc.). `CLAUDE.md` is a symlink to this file.

## What Metis is

A local-first AI dev agent server. Provider-agnostic via a canonical message format, with a layered routing engine (manual / configured rules / learned patterns), bounded portable memory, and an event bus that feeds a trace store. Python core server; thin clients (CLI first, TUI/Tauri later).

**Status:** Phase 1 + Phase 2 + Phase 2.5 + Phase 3 shipped (owner-ratified 2026-05-16); Wave 16 reaches the GA launch milestone for the first paid cohort. The three Phase-3 wedges — transparent HTTP gateway, multi-user / per-team attribution, and evaluator — are live end-to-end with buyer-facing value. Wave 15 closed both pre-GA blockers (NETWORK escalation now requires repeated transient failures; SDK-canonical bare model names normalize before routing), shipped the Stripe-backed billing module, observability extensions, concierge tools, and the status-page deployment recipe. Wave 16 turns those primitives into launch operations: billing self-service portal + plan changes + failed-payment grace, first-customer concierge scaffolding + anonymized reports + case-study templates, and day-1 operational playbooks + status-page config artifacts. **Differentiator posture:** delegation is the validated routing-surface GTM lever, with three cost-per-quality measurements at 8.3%, 19.9%, and 26.1% better than sonnet-only baseline on the fan-out workload. Slot-4 model selection remains a proof-of-mechanism, not a generalized regime: §A3-rev3 produced one sonnet inversion, while §A3-rev7 completion produced zero sonnet picks across 36 routing decisions and ruled out the last rubric/knob repair candidate on the dev-loop suite. The optional task-domain §A3 wedge is deferred post-GA. 1771 tests passing.

**Repo shape.** uv-workspace monorepo with a single published package `metis` at [`packages/metis/`](packages/metis/). Internally organized into four subpackages: `metis.core` (canonical types, events, adapters, routing, tools, memory, sessions, pricing, skills, trace, eval, patterns), `metis.server` (HTTP/WS agent server), `metis.gateway` (transparent provider-shape HTTP proxy), and `metis.cli` (chat / tui / serve / gateway entry point). The `metis` console-script is shipped by `metis.cli`. The four-package shape was consolidated into one PyPI distribution on 2026-05-19 to prioritize Persona B (individual AI engineer running `pip install metis && metis chat .`); see [`docs/operations/repo-split-plan.md`](docs/operations/repo-split-plan.md) and the related [memory entry](../.claude/projects/-Users-2sumai-git-metis/memory/project_metis_persona_b_priority.md).

## Implementation status

**What works:**

- **Canonical types** ([packages/metis/src/metis/core/canonical/](packages/metis/src/metis/core/canonical/)) — `Message`, `Role`, content blocks (Text, Image, ToolUse, ToolResult, Thinking, RedactedThinking), `ToolDefinition`, `AdapterCapabilities`, `Usage`, `RoutingDecisionRecord`. `msgspec`-backed, JSON-roundtrippable, fully validated against canonical-format §5.
- **Event bus + trace store** ([packages/metis/src/metis/core/events/](packages/metis/src/metis/core/events/), [packages/metis/src/metis/core/trace/](packages/metis/src/metis/core/trace/)) — full catalog (31 event types across session/turn/llm/tool/route/skill/memory/pattern/eval/bus domains), bounded async dispatch, fast-path / non-fast-path subscribers, SQLite WAL+NORMAL writer, replay query, causal-chain walk. Bus lifecycle is observable: `bus.subscriber_registered` / `bus.subscriber_unregistered` fire on every `subscribe()` / unsubscribe, and `bus.gap_detected` fires when the per-subscription monotonic frame counter detects a dropped event (overflow on a bounded queue).
- **Tool dispatcher** ([packages/metis/src/metis/core/tools/](packages/metis/src/metis/core/tools/)) — registry + JSON-Schema validation + confirmation policy + 5 file/shell builtins + 3 memory tools. Workspace-scoped file API rejects `..` and out-of-root symlinks.
- **Provider adapters** — Anthropic ([adapters/anthropic.py](packages/metis/src/metis/core/adapters/anthropic.py)), OpenAI ([adapters/openai.py](packages/metis/src/metis/core/adapters/openai.py)), OpenRouter ([adapters/openrouter.py](packages/metis/src/metis/core/adapters/openrouter.py)). Each implements wire translation, error classification (8-class), bounded retry with `retry_after`, cancellation, per-model `AdapterCapabilities`, and `stream()` returning canonical streaming events. Cross-provider continuity verified end-to-end against real APIs (Anthropic→OpenAI→OpenRouter mid-session with tool-use round-trip).
- **Routing engine** ([packages/metis/src/metis/core/routing/](packages/metis/src/metis/core/routing/)) — full 7-slot chain (per-message override / manual sticky / configured rules / pattern / delegate [stub] / workspace default / global default) with capability validation and per-(provider, model) availability tracking. Emits exactly one `route.decided` event per turn, including on hard failure. YAML rule policy is parsed from `<workspace>/.metis/routing.yaml` (per `routing-engine.md §5`); the `rule` slot reports a real verdict, not `not_applicable`. Slot 4 (`pattern`) consults the per-workspace `PatternStore` when a resolver is injected and emits `pattern.matched` on a win.
- **Session manager** ([packages/metis/src/metis/core/sessions/manager.py](packages/metis/src/metis/core/sessions/manager.py)) — turn-locked streaming model loop, multi-call within a turn, tool cycle wiring, cost stamping, full event emission, optional streaming-event handler for live token deltas.
- **Bounded memory** ([packages/metis/src/metis/core/memory/](packages/metis/src/metis/core/memory/)) — per-workspace `MEMORY.md` (~2 KB) and `USER.md` (~1.5 KB) under `.metis/`. Soft cap → `memory.eviction` event; hard cap → write rejected. `memory_add`/`memory_replace`/`memory_consolidate` tools mutate them. `SessionManager` composes USER.md + MEMORY.md into the system prompt fresh each LLM call.
- **Session message persistence** ([packages/metis/src/metis/core/sessions/sqlite_store.py](packages/metis/src/metis/core/sessions/sqlite_store.py)) — SQLite-backed session/message store per `canonical-message-format.md §9.1`. Wired by default in the CLI runtime; `InMemorySessionStore` remains for tests.
- **HTTP/WebSocket server** ([packages/metis/src/metis/server/](packages/metis/src/metis/server/)) — Starlette + uvicorn ASGI app. REST endpoints for sessions/turns/messages/models/health; WebSocket `/sessions/{id}/stream` with single-use attach tokens, snapshot+live, `preset:chat`/`preset:full` filters, cancel-via-WS, ping/pong. Streaming-only events (`message.*`, `text.delta`, `tool.use_*`) flow through a per-session `StreamingHub` and reach connected clients live; bus catalog events flow through the normal Subscription path. Loopback-only bind in v1.
- **CLI** ([packages/metis/src/metis/cli/](packages/metis/src/metis/cli/)) — `metis chat <workspace>` (line REPL), `metis tui <workspace>` (Textual TUI), `metis serve <workspace>` (HTTP/WS server), `metis gateway` (transparent gateway server) and `metis gateway issue-key --name … --workspace …` (key issuance). Slash commands `/model`, `/cost`, `/models`, `/help`. Per-message `@alias` override syntax.
- **Transparent HTTP gateway** ([packages/metis/src/metis/gateway/](packages/metis/src/metis/gateway/)) — Starlette ASGI app exposing `POST /v1/chat/completions` (OpenAI shape) and `POST /v1/messages` (Anthropic shape), each in sync + SSE flavors. Authenticates per-request via `Authorization: Bearer gw_…` (or `x-api-key` for Anthropic clients), maps the bearer hash to a `GatewayKey` scoped to one workspace, routes via `metis_core.routing.RoutingEngine`, and writes `route.decided` / `llm.call_started` / `llm.call_completed` / `turn.completed` events with `gateway_key_id` + `inbound_shape` stamped on the LLM/turn payloads. Per-request stateless — no session manager, no tool dispatcher, no memory store (gateway.md §2). Loopback-only bind. Keys live in `~/.metis/gateway/keys.json` (default); the plaintext token is printed once by `metis gateway issue-key` and only the SHA-256 hash is persisted.
- **Smoke harnesses** ([scripts/smoke.py](scripts/smoke.py), [scripts/smoke_cross_provider.py](scripts/smoke_cross_provider.py), [scripts/smoke_cache.py](scripts/smoke_cache.py)) — drive the loop against real APIs; cross-provider test passes at ~$0.007.
- **Configured routing rules** ([packages/metis/src/metis/core/routing/policy.py](packages/metis/src/metis/core/routing/policy.py)) — per-workspace YAML rule file at `<workspace>/.metis/routing.yaml`; matches turn inputs against rule predicates and emits a real verdict in the `rule` slot of `route.decided.chain` (shipped commit `e71fedd`).
- **Tool-confirmation REST endpoint** — `POST /turns/{id}/confirmations/{request_id}` wired against a per-turn request registry bridged to the `tool.confirmation_requested` event stream (shipped commit `e71fedd`).
- **CLI confirmation handler** ([packages/metis/src/metis/core/tools/cli_confirmation.py](packages/metis/src/metis/core/tools/cli_confirmation.py)) — terminal-prompting handler wired as the default for `metis chat` / `metis tui`. NONE/READ side effects auto-approve; WRITE/EXECUTE/NETWORK consult `<workspace>/.metis/trust.yaml` (`always_allow` / `always_deny` lists) and otherwise prompt the user, with "always" / "never" answers persisting back to the file. `--auto-allow` reverts to the old `AutoAllowHandler`.
- **Persisted skills** — `SkillStore` and the `skill_load` tool, agentskills.io-compatible (shipped commit `e71fedd`).
- **Savings benchmark suite** ([scripts/benchmark.py](scripts/benchmark.py), [benchmarks/workloads/](benchmarks/workloads/)) — versioned workload bundle + harness; six workloads in suite v1 (`fix-a-bug-small`, `multi-turn-refactor`, `write-a-doc-from-notes`, `intentionally-failing-task`, plus diversity-wave additions `regex-with-edge-cases` and `multi-file-refactor-with-shared-types`). The two diversity workloads are deliberately failure-prone in the haiku case so per-workload quality scores discriminate models. Run 3 captured in [benchmarks/RESULTS.md](benchmarks/RESULTS.md). Spec at `docs/specs/benchmark.md`.
- **Prompt caching (universal, live-validated)** — Anthropic `cache_control` breakpoints attached to the stable prefix (tools + system); v2 of the context assembler adds a minimum-cacheable-prefix rule that pads the prefix above the effective haiku-4-5 cache floor (`MIN_CACHEABLE_PREFIX_TOKENS = 4500`, `MAX_CACHEABLE_PREFIX_TOKENS = 5500` heuristic tokens; padding sources are loaded skills first, then a deterministic `_OPERATING_CONTEXT_PADDING` block). Spec at `docs/specs/context-assembler.md §5.1`. Two-segment `system_prompt` field on `CanonicalRequest` separates stable vs mutating instructions. Benchmark Run 3 (see [benchmarks/RESULTS.md §Run 3](benchmarks/RESULTS.md)): cache fires on **49 of 49 LLM calls (100%)** in the 6-workload suite vs Run 2 cold's **10 of 30 (33%)**; same-3-workload aggregate cost dropped 22.8%.
- **Analytics + dashboard** — `AnalyticsStore.savings()` and the metric dashboard surface ([packages/metis/src/metis/server/](packages/metis/src/metis/server/)); spec at `docs/specs/analytics-api.md`.
- **Pattern store** ([packages/metis/src/metis/core/patterns/](packages/metis/src/metis/core/patterns/)) — per-workspace, bounded SQLite store of structural fingerprints + Welford-accumulated outcomes (cost, latency, success score). Wires routing slot 4 (`PATTERN_RECOMMENDATION`) when a `pattern_store_resolver` + `fingerprint_inputs_builder` are injected; emits `pattern.recorded` / `pattern.matched` / `pattern.evicted`. Spec at `docs/specs/pattern-store.md`.
- **Evaluator (heuristic + LLM + hybrid tiers)** ([packages/metis/src/metis/core/eval/](packages/metis/src/metis/core/eval/)) — bus subscriber + `HeuristicJudge` / `LLMJudge` / `HybridJudge` for turn / tool-cycle / session / workload subjects. Subscribes to `turn.completed` / `tool.completed` / `tool.failed` / `session.ended` and emits `eval.started` / `eval.completed` / `eval.failed` per `docs/specs/evaluator.md`. `metis evaluate --db-path … --subject turn` re-runs the judge over a window for batch re-evaluation; `workload.yaml.evaluate` feeds the benchmark suite's quality column. `BudgetTracker` (per-session $0.10 / per-day $1.00 defaults) is shared across LLM-eligible judges; over-budget calls land a `signals.budget_exhausted=True` verdict and HybridJudge falls back to its heuristic verdict. `HybridJudge(escalation_threshold=0.7)` is the default — heuristic confidence at or above the threshold short-circuits the LLM call. Tool-cycle and session subjects remain heuristic-only in v1 (evaluator.md §5.5 / §5.6).
- **Quality analytics** — `/analytics/quality` endpoint projects `eval.completed` over a time window with `group_by` ∈ {model, judge_kind, rubric_id, none} and `min_confidence` filter. The `chosen_model` field joins via `route.decided` so the per-model rollup shows which *judged* model scored best, not the judge's own model. Spec at [docs/specs/evaluator.md §9.2](docs/specs/evaluator.md).
- **Per-gateway-key analytics** — `/analytics/cost?group_by=gateway_key` and `/analytics/by_key` roll up `llm.call_completed` / `turn.completed` cost + tokens + call-count by `gateway_key_id`, with an optional `gateway_key` exact-match filter. Direct-API calls (no stamp) bucket under `gateway_key_id: null`. Closes the Wave-5 follow-on noted in `gateway.md §V`.
- **Gateway client quickstart** — [docs/gateway-client-quickstart.md](docs/gateway-client-quickstart.md) walks Claude Code / Cursor / raw SDK clients through pointing `ANTHROPIC_BASE_URL` / `OPENAI_BASE_URL` at a running gateway, with runnable examples under [examples/gateway/](examples/gateway/).
- **Benchmark harness LLM-judge integration** — `scripts/benchmark.py` exposes `--judge {heuristic,hybrid,llm}`, `--judge-escalation-threshold`, and `--judge-model` flags that swap the per-turn and workload-level evaluator's `Judge` between `HeuristicJudge` / `HybridJudge` / `LLMJudge` without touching `metis-core`. First measured end-to-end in [benchmarks/RESULTS.md §A3](benchmarks/RESULTS.md) — a three-pass experiment (haiku → sonnet → no-active-model, sharing one patterns DB) under `--judge hybrid --judge-escalation-threshold 0.7`. Pass C lands slot 4 wins on 17 of 18 turns reading cross-model outcomes; the differentiator does **not** invert in v1 (every win picks haiku), and §A3 identifies the two specific unblocks (heuristic needs a `tool.completed.success=False` penalty; the session manager needs to forward `assistant_response_text` on `turn.completed.signals_extra` so the LLM judge can read it).
- **§A3-rev evidence** — both §A3 unblocks have landed (heuristic `weight_no_tool_exit_failure` and `SessionManager` `signals_extra` plumbing for `user_prompt_text` + `assistant_response_text`); a follow-up three-pass run is captured in [benchmarks/RESULTS.md §A3-rev](benchmarks/RESULTS.md). The unblocks fire as intended at the per-turn level (15 hybrid escalations across the three passes vs 0 in §A3-original; LLM judge returns differentiated 0.3/0.7/0.8/1.0 scores), but slot 4 in Pass C still picks haiku on all 15 routed turns — the K-NN aggregation across mixed-workload clusters + `cost_weight=0.3` consistently produces haiku-aggregated scores 0.755–1.000 vs sonnet-aggregated 0.245–0.700. §A3-rev identifies a third blocker (workload-grain K-NN clustering or `cost_weight` reduction). Total spend: $1.032.
- **§A3-rev2 evidence** — all three §A3-rev unblocks have landed (Wave 8a-1 workload-tagged fingerprint, 8a-2 `cost_weight=0.3 → 0.1`, 8a-3 grounding-check rubric primitive replacing the single-substring check on `architectural-explanation-without-hallucination`); a follow-up three-pass run is captured in [benchmarks/RESULTS.md §A3-rev2](benchmarks/RESULTS.md). The unblocks individually work as designed: workload-tag partitioning gives clean clusters (same-workload neighbors score ≥ 0.85 vs different-workload ≤ 0.15), `cost_weight=0.1` lowers the success-delta needed to flip the chooser to ~0.143, and the grounding-check eliminates §A3-rev's artifact where sonnet was wrongly penalized 0.50 on the hallucination workload (now 0.90 / 0.95). Pass B was re-run on three workloads that hit Anthropic transient connection errors so the patterns DB had 5-11 samples per (workload, model). Pass C: **slot 4 emits `not_applicable` on all 18 turns** and slot 7 (`global_default`) wins. The K-NN does read cross-model data correctly (e.g. on `write-a-doc-from-notes` Pass C turn 2, sonnet's aggregated score 0.900 is ahead of haiku 0.842 — the first time in any A3 series), but the confidence gap formula `(top-runner)/top` produces values 0.030–0.231 under `cost_weight=0.1`, all below the unchanged `min_confidence=0.3` gate. The three correct unblocks combined to gate slot 4 off entirely rather than inverting it. Wave 9 candidate: lower `PatternConfig.min_confidence` to ~0.05 (or reshape the confidence formula) — at 0.05 the `write-a-doc-from-notes` turn 2 inversion would have fired. Total spend: $1.316.
- **Gateway multi-user identity (Wave 8a-4 + 8a-5 + 8a-6)** — `GatewayKey` ([`packages/metis/src/metis/gateway/auth.py`](packages/metis/src/metis/gateway/auth.py)) gains optional `user_id` / `team_id` fields (`^[a-z0-9_-]+$`, ≤200 chars); `metis gateway issue-key --user <id> --team <id>` persists them. A request-scoped `Identity` projects `(gateway_key_id, workspace_path, user_id, team_id)` per multi-user.md §3.2 and the gateway harness stamps `user_id` / `team_id` as typed fields on `llm.call_completed` and `turn.completed`. The analytics surface reads them: `/analytics/cost?group_by=user|team`, `/analytics/by_user`, `/analytics/by_team` roll up cost / tokens / call-counts per identity (multi-user.md §4.1, analytics-api.md §4.1 / §4.9). Pre-multi-user keys keep `user_id: None` / `team_id: None` and bucket under the null-row convention. Agent-loop traffic (`metis chat` / `metis serve`) also stamps `None`. Closes the Wave-7-deferred multi-user-rollup gap noted in `gateway.md §11`.
- **§A3-rev3 evidence — the differentiator inverts** — Wave 9's one-line knob (`PatternConfig.min_confidence: 0.3 → 0.05`, [`routing/policy.py:63`](packages/metis/src/metis/core/routing/policy.py#L63)) is live and a follow-up three-pass run is captured in [benchmarks/RESULTS.md §A3-rev3](benchmarks/RESULTS.md). Pass C reaches slot 4 on **14 of 18 turns** (vs §A3-rev2's 3 of 16) and **picks sonnet on `regex-with-edge-cases` turn 2** — the hard "16-test edge cases" turn where haiku rubric-fails 0.19@0.80. Cluster: haiku 0.784, sonnet 0.833, confidence 0.058 (would have been rejected under §A3-rev2's 0.3 gate). Pass C aggregate `savings_pct=62.0%` (vs flat-haiku 66.7%), quality sum 5.55 beats Pass A's 5.16 (+0.39, mostly from regex 0.19 → 0.74). Cost-per-quality $0.0477 lands inside the headline window between haiku-only $0.0383 and sonnet-only $0.1176. First end-to-end demonstration of differentiated routing in any A3 series. Total spend: $1.138.
- **Delegation v1 MVP (Wave 10)** — The `delegate()` built-in tool ([`packages/metis/src/metis/core/tools/builtins/delegate.py`](packages/metis/src/metis/core/tools/builtins/delegate.py)) and the worker-session lifecycle in [`SessionManager.spawn_worker`](packages/metis/src/metis/core/sessions/manager.py) ship end-to-end per [`delegation.md`](docs/specs/delegation.md) §4-7. Planner sessions whose active model has `can_delegate=True` see the tool; emitting a `delegate(tier, task, context)` call spawns a worker `Session` (`is_worker=True`, `parent_session_id` / `parent_tool_use_id` set, workspace inherited), runs it synchronously, returns the worker's output as the tool result, and lets the planner's next turn integrate. Routing slot 5 (`DELEGATE_REQUEST`) fires `chose: <tier model>` inside worker re-entry via `TurnContext.worker_tier_model`; top-level sessions still see `not_applicable`. Slot 4 (pattern) defers with `reason="delegate_request_in_flight"` so a learned pattern can't override the planner's explicit `tier=` choice (delegation.md §11). Worker LLM events stamp `parent_session_id` and use `Actor.WORKER`; `/analytics/cost` rolls worker spend under the planner via `group_by=parent_session` or partitions via `group_by=is_worker`, with `?include_workers=false` for planner-direct views. Three new bus events: `delegate.started` / `delegate.completed` / `delegate.failed` (PSEUDONYMOUS). Worker tool isolation: `delegate` + memory tools (`memory_add` / `memory_replace` / `memory_consolidate`) filtered out of worker dispatch (§5.6); CLI confirmation handler treats worker "always" answers as one-time approvals so worker prompts can't persist into `trust.yaml` (§13). **First end-to-end validation (§A3-rev5 Q2, 2026-05-15):** delegation produces **8.3% better cost-per-quality** on the `multi-step-with-delegation` workload designed to exercise fan-out — sonnet planner + haiku workers at $0.221 / quality 0.91 = **$0.243/quality-unit**, vs sonnet-only-no-delegation at $0.183 / quality 0.69 = $0.265/quality-unit. 3 `delegate.started` events fire reliably per turn; the 23.9% headline `savings_pct` is the analytics counterfactual (worker tokens repriced at sonnet rates), absolute delegation cost is higher than sonnet-only-no-delegation but quality is higher too. First end-to-end demonstration of delegation moving cost-per-quality in any A3 series. **Deferred to later waves:** async/concurrent workers (synchronous-only in v1), cancellation cascade (planner cancel doesn't propagate to in-flight workers), streaming worker output back to the planner, recursive (worker-spawns-worker) delegation, `output_schema` validation, per-tier worker timeout, router-decided delegation (slot 5 only fires inside worker re-entry, never as the entry point), and worker pattern-store integration (workers don't record/match patterns in v1). 17 new tests under `packages/metis/tests/core/workers/test_delegation.py` + 3 new analytics rollup tests. See delegation.md §3.6 for the canonical deferral list.
- **Pattern store v2 — hybrid embedding fingerprint (Wave 10) + recording-side wired end-to-end (Wave 11)** — Opt-in alternative to the v1 structural fingerprint per [`pattern-store.md §16`](docs/specs/pattern-store.md). Module [`packages/metis/src/metis/core/patterns/embeddings.py`](packages/metis/src/metis/core/patterns/embeddings.py) defines a `@runtime_checkable` `EmbeddingProvider` Protocol and three concrete providers (`openai:text-embedding-3-small` 1536-dim, `cohere:embed-multilingual-v3.0` 1024-dim, `local:sentence-transformers:all-MiniLM-L6-v2` 384-dim) plus a `DeterministicEmbeddingProvider` for tests and a `resolve_embedding_provider(provider_id)` registry. `PatternStore` gains an additive `embedding_cache(text_sha256, provider_id, embedding_blob, embedding_dim, …)` table — keyed `(provider_id, SHA-256(user_message_text))`, vector blobs packed via `array.array('f', …).tobytes()` (no NumPy dep), bounded by `embedding_cache_max_rows=10_000` + `embedding_cache_max_age_days=180` with age-first → LRU → use-count tie-break eviction. Blended similarity: `α × cosine + (1 − α) × weighted_jaccard` with default `α = 0.6`; mixed-version K-NN (v1 row vs v2 query) falls back to v1 weighted-Jaccard. Opt in via `PatternConfig.fingerprint_version="v2"` + `embedding_provider="<provider_id>"` in `routing.yaml`; v1 default is unchanged. **Wave 11 closes the §A3-rev4 Q1 recording-side gap**: embedding compute moves to **turn-start** (the SessionManager's `fingerprint_inputs_hook` becomes async-capable, with [`packages/metis/src/metis/cli/runtime.py`](packages/metis/src/metis/cli/runtime.py) awaiting `attach_embedding_for_recording` BEFORE `turn.started` fires), so the inputs reach the pattern subscriber's `_on_turn_completed` already carrying `embedding` + `embedding_provider` and `compute_fingerprint` produces a HYBRID row natively. The Wave-10 post-record warm-up in [`packages/metis/src/metis/core/patterns/subscriber.py`](packages/metis/src/metis/core/patterns/subscriber.py) is removed; the recording path is now purely synchronous between `record()` and `_turn_outcomes[turn_id]` being set, preserving the §A3-rev3 eval-cascade invariant by construction. Routing slot 4 in v2 mode does a sync cache-only lookup before computing the query fingerprint — cache hit → blended K-NN (the cache is now populated at turn-start, so v2 K-NN actually reads cosine + jaccard end-to-end); cache miss → v1 jaccard. Recording path remains async-capable via `attach_embedding_for_recording()` (driven from the hook, not the subscriber). Schema_version bumps `"1" → "2"` in place; no row migration. **62 tests under `packages/metis/tests/core/patterns/`**: 52 from Wave 10 (Protocol contract, cosine/blend math, cache TTL + LRU eviction, mixed-version K-NN, recording-path cache-first embed, routing slot-4 v2 path) + 10 from Wave 11 (4 in `test_v2_recording_wiring.py` covering async hook awaited by manager, end-to-end HYBRID row written via SQL inspection, eval cascade preserved, defensive STRUCTURAL fallback; 6 in `test_v2_cluster_tightening.py` covering §16.10 test 5 headline gates intra Δ ≥ 0.10 / inter Δ ≥ 0.05 on a 60-turn fixture spanning 6 workloads × 10 + 4 off-benchmark traces, plus fixture-sanity tests and an α-sweep). Deviations from the original spec recorded in pattern-store.md §16.13 (rename `embedding_blend_alpha → embedding_alpha`, no `embedding_strategy` knob, sync `recommend()` preserved, no NumPy hard dep). **Post-Wave-11 §A3-rev5 readout (2026-05-15):** v2 K-NN now fires end-to-end at routing time — every Pass C `pattern.matched` reports `fingerprint_kind="hybrid"`, no v1 fallback (verified: 18 of 18 fingerprints in `a3rev5-patterns.db` are HYBRID, vs §A3-rev4's 70 of 70 STRUCTURAL). However, **the §A3-rev3 regex inversion did not reproduce**: per-fingerprint K-NN clusters are dominated by haiku's 2-3× sample-size advantage under `cost_weight=0.1`, so Pass C produces 0 pattern-slot sonnet picks across 17 routed turns. v2 HYBRID embeddings are a *necessary* foundation for further cluster-tightening (per-prompt fingerprint partitioning, or `cost_weight` reduction below 0.1) but not sufficient by themselves — Wave 12 candidate.
- **API versioning posture (Wave 10) + deprecation enforcement (Wave 11)** — [`api-versioning.md`](docs/specs/api-versioning.md) v1 pins two surface categories: provider-shape endpoints (`/v1/chat/completions`, `/v1/messages` — frozen by upstream SDK contracts; Metis doesn't version them) and Metis-owned endpoints (`/healthz`, `/health`, `/server/version`, `/sessions/*`, `/analytics/*`, `/models`, future Metis-specific surfaces — versioned by us via the `Metis-API-Version` header). Pure ASGI middleware in [`packages/metis/src/metis/gateway/middleware_versioning.py`](packages/metis/src/metis/gateway/middleware_versioning.py) and [`packages/metis/src/metis/server/middleware_versioning.py`](packages/metis/src/metis/server/middleware_versioning.py) (not `BaseHTTPMiddleware`, which would buffer SSE / WebSocket bodies); both expose `CURRENT_VERSION = "1.0"`, `MIN_SUPPORTED_VERSION`, `DEPRECATED_VERSIONS: dict[str, str]` (version → ISO sunset date; empty in v1), and `resolve_version(requested) -> VersionResolution(resolved, is_deprecated, sunset, reason)`. **Wave 11 lands the enforcement loop on top of Wave 10's scaffolding** (api-versioning.md §3 / §6): versions listed in `DEPRECATED_VERSIONS` are served with `Deprecation: true` + `Sunset: <ISO date>` response headers per RFC 8594 while inside the window; both past-sunset entries (`reason="past_sunset"`) and below-`MIN_SUPPORTED_VERSION` requests (`reason="below_min"`) are rejected outright with **HTTP 410 Gone** and the documented `version_unsupported` body. `OPTIONS` requests short-circuit with 204 + the version-negotiation headers so callers can probe support without a real request. Gateway middleware defaults to skipping the provider-shape prefixes so SDK clients see byte-identical responses. 20 new tests in Wave 10 + additional Wave 11 enforcement tests across both apps cover the 410 / 426 / 204 paths and the deprecation-header window.
- **Trace DB backup/restore (Wave 10)** — `metis backup <dest> [--db-path <source>]` and `metis restore <source> [--db-path <dest>] [--force]` CLI subcommands per [`event-bus-and-trace-catalog.md §7.5`](docs/specs/event-bus-and-trace-catalog.md). Module [`packages/metis/src/metis/core/trace/backup.py`](packages/metis/src/metis/core/trace/backup.py) uses SQLite's `VACUUM INTO` (atomic, WAL-safe, single-file output; source DB stays open and writable). `TraceStore` stamps `PRAGMA user_version = TRACE_SCHEMA_VERSION` on every opened trace DB so the restore path can refuse schema-version mismatches. `restore()` defaults to refusing-overwrite (`--force` to opt in), refuses if `-wal` / `-shm` companions sit alongside the source backup. Both commands emit a deterministic metadata block on success (source / dest / byte count / schema version / event count / oldest+newest event timestamps; no random ids). 18 new tests (13 library + 5 CLI) including 100k-event backup performance.
- **Gateway key lifecycle (Wave 10)** — `metis gateway revoke-key <key_id>`, `metis gateway rotate-key <key_id> [--grace-period <duration>]`, `metis gateway list-keys [--format text|json]` CLI subcommands close the v1 "no online revocation or rotation" gap per [`gateway.md §11`](docs/specs/gateway.md). `GatewayKey` ([`packages/metis/src/metis/gateway/auth.py`](packages/metis/src/metis/gateway/auth.py)) gains `status: Literal["active", "revoked"]`, `revoked_at: datetime | None`, `grace_period_until: datetime | None`; loader is back-compat. New module [`packages/metis/src/metis/gateway/keystore_admin.py`](packages/metis/src/metis/gateway/keystore_admin.py) exposes `revoke_key` / `rotate_key` / `list_keys` / `sweep_expired_grace_periods` plus `parse_duration("30m"|"24h"|"7d"|"2w")`. All mutations use atomic write-temp-then-rename. Rotation default grace period: 24h; both old and new keys are valid during the window; predecessor auto-revokes at the boundary. Three new audit event types (PSEUDONYMOUS floor): `gateway.key_issued` / `gateway.key_revoked` (with `reason: Literal["admin_revoke", "grace_period_expired", "rotated"]`) / `gateway.key_rotated`. Auth middleware returns a documented 401 `key_revoked` body shape (matches the existing OpenAI vs Anthropic envelopes). 27 new tests.
- **Prometheus `/metrics` endpoint (Wave 11)** — `GET /metrics` on both the gateway ([`packages/metis/src/metis/gateway/app.py`](packages/metis/src/metis/gateway/app.py)) and the server ([`packages/metis/src/metis/server/app.py`](packages/metis/src/metis/server/app.py)) exposes Prometheus exposition for in-cluster scrapers, per [`docs/specs/observability.md`](docs/specs/observability.md). Bus-driven collector at [`packages/metis/src/metis/core/observability/metrics.py`](packages/metis/src/metis/core/observability/metrics.py) — non-fast-path subscriber that projects 7 catalog events (`llm.call_completed` / `llm.call_failed` / `route.decided` / `pattern.matched` / `quota.alert` / `gateway.quota_exceeded` / `eval.completed`) onto a private `prometheus_client.CollectorRegistry` (default `REGISTRY` is process-global and would raise on duplicate-metric across collectors). Ten metric series: counters `metis_llm_calls_total{provider,model,status}` / `metis_llm_cost_usd_total{provider,model}` / `metis_routing_decisions_total{winning_slot,chosen_model}` / `metis_pattern_matches_total{chose_model,fingerprint_version}` / `metis_eval_verdicts_total{judge_kind,subject_kind}`; histogram `metis_llm_call_latency_seconds{provider,model}` (11 buckets, 50ms-120s); gauges `metis_quota_used_ratio{identity_kind,identity_id}` / `metis_session_count` (server only, polled from `runtime.session_store`) / `metis_gateway_keys_active` / `metis_gateway_keys_revoked` (gateway only, polled from `runtime.keystore` with grace-period-expired counted as revoked even before the next admin sweep). Best-effort: handler exceptions are logged and swallowed — observability never blocks a turn. Helm template at [`infra/gateway/helm/templates/servicemonitor.yaml`](infra/gateway/helm/templates/servicemonitor.yaml) emits a `monitoring.coreos.com/v1 ServiceMonitor` for Prometheus-Operator clusters, gated by `serviceMonitor.enabled` in `values.yaml`. 26 new tests (15 in [`packages/metis/tests/core/observability/test_metrics.py`](packages/metis/tests/core/observability/test_metrics.py) + 6 in [`packages/metis/tests/gateway/test_metrics_endpoint_gateway.py`](packages/metis/tests/gateway/test_metrics_endpoint_gateway.py) + 5 in [`packages/metis/tests/server/test_metrics_endpoint_server.py`](packages/metis/tests/server/test_metrics_endpoint_server.py)). Closes the long-standing gateway.md §11 "no `/metrics` endpoint" deferral.
- **Rate-limit middleware (Wave 11)** — Per-key + per-IP token-bucket rate limits on the gateway, per [`docs/specs/gateway-hardening.md §3`](docs/specs/gateway-hardening.md). Pure ASGI middleware at [`packages/metis/src/metis/gateway/middleware_ratelimit.py`](packages/metis/src/metis/gateway/middleware_ratelimit.py) (not `BaseHTTPMiddleware`, to preserve SSE streaming); two independent buckets gate every authenticated request — the per-key bucket keys on `gateway_key_id` (post-auth), the per-IP bucket keys on the ASGI socket peer. Either bucket exhausted → **HTTP 429** with a `Retry-After` header and an inbound-shape-matched error envelope (OpenAI's `{"error": {...}}` vs Anthropic's `{"type": "error", "error": {...}}`), so SDK clients surface a typed retry instead of a parse failure. `RateLimitConfig` (capacity = refill-per-window; default disabled) is configurable via helm values; opt-in via `RateLimitConfig(enabled=True)`. 18 new tests in [`packages/metis/tests/gateway/test_middleware_ratelimit.py`](packages/metis/tests/gateway/test_middleware_ratelimit.py) cover bucket exhaustion, per-key vs per-IP independence, 429 envelope shape per inbound, `Retry-After` math, and disabled-config pass-through. Closes the gateway.md §11 "no rate limiting" deferral.
- **Operations docs (Wave 11)** — [`docs/operations/`](docs/operations/) ships the buyer-facing operational playbooks an SRE will read before signing: [`incident-response.md`](docs/operations/incident-response.md) (SEV1-SEV4 criteria, on-call alert paths for PagerDuty / Opsgenie / email, first-hour playbook, post-mortem template, per-failure-mode playbooks for upstream LLM outage, trace-DB corruption, gateway-key compromise, quota runaway); [`sla-template.md`](docs/operations/sla-template.md) (99.5% single-region template with service-credit math, exclusions, force-majeure stub deferred to legal counsel); [`status-page.md`](docs/operations/status-page.md) (two-tier recipe — external UptimeRobot / Statuspage.io / Better Stack against `/healthz` plus self-hosted Uptime Kuma in-cluster, publish/redact guidelines, incident comm templates); [`upgrade-guide.md`](docs/operations/upgrade-guide.md) (rolling-upgrade procedure on the helm chart, backup-before-upgrade pattern via `metis backup`, rollback steps, version-skew matrix). Closes the gateway.md §11 "no operational documentation" and "no upgrade path" deferrals.
- **Audit log (Wave 12)** — [`docs/specs/audit-log.md`](docs/specs/audit-log.md) v1 + [`AUDIT_EVENT_TYPES`](packages/metis/src/metis/core/events/payloads.py) frozenset (12-type subset: `gateway.key_issued` / `gateway.key_revoked` / `gateway.key_rotated` / `gateway.quota_exceeded` / `quota.alert` / `routing.policy_invalid` / `memory.eviction` / `pattern.evicted` / `tool.confirmation_resolved` / `trace.swept` / `analytics.user_exported` / `analytics.user_forgotten`). New `metis audit export <dest> [--since <iso>] [--until <iso>] [--format jsonl|csv] [--event-type <name>] [--redact <mode>]` CLI ([`packages/metis/src/metis/cli/audit.py`](packages/metis/src/metis/cli/audit.py)) projects the audit subset from the existing trace store and writes deterministic JSONL or CSV for SIEM ingest. Audit log is a filtered projection, not a parallel write path — the trace store stays the source of truth (audit-log.md §2). The retention sweep (Wave 12a-2) reads `AUDIT_EVENT_TYPES` to exempt audit rows.
- **Trace retention (Wave 12)** — [`docs/specs/trace-retention.md`](docs/specs/trace-retention.md) v1 + `TraceStore.purge_older_than(cutoff, dry_run=True)` ([`packages/metis/src/metis/core/trace/retention.py`](packages/metis/src/metis/core/trace/retention.py)) implements sliding-window retention with audit-event exemption. New `metis trace prune [--days 90] [--dry-run] [--db-path …]` CLI subcommand ([`packages/metis/src/metis/cli/trace_admin.py`](packages/metis/src/metis/cli/trace_admin.py)) and a Helm CronJob template ([`infra/gateway/helm/templates/cronjob-trace-prune.yaml`](infra/gateway/helm/templates/cronjob-trace-prune.yaml)) that runs `metis trace prune` on schedule (off by default; opt-in via values.yaml). New `trace.swept` audit event records each prune run so a later sweep with a cutoff past the first sweep's timestamp does not delete the audit trail of the prune mechanism itself (audit-log.md §4). 13a-5 (Wave 13) added `metis trace vacuum` + a `metis_trace_wal_bytes` gauge.
- **Redaction layer (Wave 12)** — [`docs/specs/redaction.md`](docs/specs/redaction.md) v1 + [`packages/metis/src/metis/core/redaction/`](packages/metis/src/metis/core/redaction/) module: `RedactionMode` StrEnum (`passthrough` / `pseudonymize` / `redact_private` / `aggregate_only`), `EventRedactor(mode, *, salt=b"", strip_user_controlled=False)`, `AggregateAccumulator`, `PseudonymTag` closed catalog, declarative `PAYLOAD_PSEUDONYM_FIELDS` table covering every catalog event whose payload carries identity fields. `metis audit export --redact <mode>` opts in to redaction at export time. Pseudonymized output matches 12a-2's `pseudonym_for()` byte-for-byte so a row pseudonymized by GDPR forget and re-exported under `pseudonymize` produces the same digest. Redaction is export-time only — the append-only invariant at the recording layer is preserved; GDPR right-to-be-forgotten (§5) is the one documented exception, performing a narrowly-scoped in-place identity overwrite.
- **GDPR data export + forget (Wave 12)** — `metis analytics user-export <user_id> [--from] [--to] [--out] [--db-path]` ([`packages/metis/src/metis/cli/user.py`](packages/metis/src/metis/cli/user.py)) streams every trace event stamped with `user_id` as JSONL (Article 15 portability); `metis user forget <user_id> [--confirm]` ([`packages/metis/src/metis/core/redaction/forget.py`](packages/metis/src/metis/core/redaction/forget.py)) pseudonymizes identity fields in place via the 12a-2 `PseudonymizingRedactor` (Article 17 erasure-as-pseudonymization; aggregate analytics survives, the link to the natural person does not). Both audit-flagged: `analytics.user_exported` / `analytics.user_forgotten`. HTTP twins at `GET /analytics/user/{user_id}/export` + `POST /analytics/user/{user_id}/forget` per [`docs/specs/analytics-api.md §4.10`](docs/specs/analytics-api.md); server-side SQLite cursor yields JSONL with O(1) RAM. `metis user forget` without `--confirm` runs a dry-run preflight that prints "this would pseudonymize N event(s)" so operators can validate scope before committing.
- **SOC2 readiness gap audit (Wave 12)** — [`docs/operations/soc2-readiness.md`](docs/operations/soc2-readiness.md) maps all five Trust Service Criteria categories (Security CC1–CC9, Availability A1, Confidentiality C1, Processing Integrity PI1, Privacy P1–P8) against shipped + buyer-responsibility evidence with explicit gaps (CC8 change management, third-party pentest, vendor review, SOC2 auditor engagement). [`docs/operations/compliance-overview.md`](docs/operations/compliance-overview.md) is the one-page buyer-conversation index with the org/app/cloud responsibility model + framework table. Type 1 readiness target is Q3 2026 contingent on a buyer underwriting the audit fee; Type 2 Q4 2026 / Q1 2027.
- **`cost_weight=0.05` default landed (Wave 12)** — [`PatternConfig.cost_weight: float = 0.05`](packages/metis/src/metis/core/routing/policy.py#L76) (was `0.1` before 2026-05-15, `0.3` before 2026-05-14). Halves the haiku-cost-floor bias in the K-NN's per-cluster `score = (1 − cost_weight) × success_mean + cost_weight × cost_efficiency` so a smaller per-workload success delta is enough to flip the chooser to sonnet. §A3-rev6 (live re-run) verified the mechanical chain works as designed; see Status header for the signal-strength bottleneck this exposed.
- **Multi-tenant gateway — loopback constraint lifted (Wave 13, 13a-3)** — [`docs/specs/gateway-hardening.md`](docs/specs/gateway-hardening.md) bumps to v1-shipped. `GatewayConfig` ([`packages/metis/src/metis/gateway/app.py`](packages/metis/src/metis/gateway/app.py)) widens with `max_concurrent_connections` (default 1000), `backlog` (default 2048), `reuse_port` (default False), `tls_cert: Path | None`, `tls_key: Path | None`; `run_gateway()` no longer silently rewrites a non-loopback `--host` — non-loopback binds pass through to uvicorn and trigger a one-time `WARN` summarizing the hardening checklist (`tls_in_process=on|off rate_limit=on|off`). New CLI flags: `--host 0.0.0.0`, `--tls-cert`, `--tls-key`, `--max-connections`, `--reuse-port`. `SO_REUSEPORT` socket support enables graceful restart with two processes bound to the same port. Helm values gain `gatewayHost`, `maxConnections`, `workers`, `reusePort`, `tls.{enabled, secretName, mountPath}` plus three documented deployment recipes (Ingress / LoadBalancer / LoadBalancer + in-process TLS). Migration recipe at [`docs/operations/upgrade-guide.md §6`](docs/operations/upgrade-guide.md) with pre-flight checklist, two-step helm migration, post-migration verification, rollback path. Closes the long-standing gateway.md §3.2 / §11 "production binds require future hardening" deferral. **Breaking-default:** the silent `--host 0.0.0.0 → 127.0.0.1` rewrite is removed; callers depending on the rewrite now actually bind non-loopback. The agent server (`metis serve`) retains loopback-only until its auth story lands.
- **Benchmark-suite v2 (Wave 13, 13a-1 + 13a-2)** — [`docs/specs/benchmark.md`](docs/specs/benchmark.md) §3.1 picks up the `signal_strength: high | marginal` field on `workload.yaml` (default `"high"`); §4.1 partitions the suite into the high-signal default and the marginal historical workloads. `scripts/benchmark.py` gains `discover_workloads(include_marginal: bool = False)` + `--include-marginal` CLI flag; default suite is strict (empty without the flag in v2, since the 13a-1 audit found no v1 workload reaches the ≥ 0.15 gap threshold); explicit `--workload <name>` bypasses the filter so historical §A3 reruns reproduce. Three new high-signal candidates ship with hermetic workspace fixtures: [`subtle-bug-fix-with-test`](benchmarks/workloads/subtle-bug-fix-with-test/), [`recursive-data-structure-traversal`](benchmarks/workloads/recursive-data-structure-traversal/), [`refactor-with-contract-preservation`](benchmarks/workloads/refactor-with-contract-preservation/) — smoke results documented inline; gaps came in below the ≥ 0.4 user-brief threshold so all three are tagged `signal_strength: marginal` pending the path-3 (finer-grained outcome scoring) work. 13a-2 lands `scripts/benchmark.py --seed-passes N`: outer loop becomes nested per-(workload, rep), the inner loop inherits the prior rep's shared patterns DB so rep N+1's K-NN reads N prior samples per cluster; new `compute_workload_stats` reports `quality_mean` / `quality_std` / `cost_mean_usd` / `noisy` flag with a `NOISY_QUALITY_STD_THRESHOLD = 0.15` cutoff. The seed-passes mechanism is the harness-side path 2 for §A3-rev7 (N-shot to reduce K-NN variance); not yet exercised end-to-end against a fresh A3 run.
- **Trace-store production-readiness audit (Wave 13, 13a-5)** — [`docs/operations/trace-performance.md`](docs/operations/trace-performance.md) (~250 lines) + [`scripts/bench_trace_throughput.py`](scripts/bench_trace_throughput.py) measure the bus + trace path with synthetic `llm.call_completed` events. Reference: ~4,800 events/sec on Apple M-series / Python 3.13 / SQLite 3.50.4 / 50k events (CPU-bound at 88% wall, not disk-bound). Five additive expression indexes in [`packages/metis/src/metis/core/trace/store.py`](packages/metis/src/metis/core/trace/store.py) drive `/analytics/by_user` / `/analytics/by_team` / `/analytics/quality` / user-export from full SCAN to indexed lookup; `idx_events_turn_id_id` eliminates a temp B-TREE sort. New `metis trace vacuum` CLI + helm CronJob (off by default, default monthly schedule); WAL gauge `metis_trace_wal_bytes` on both server and gateway; `wal_autocheckpoint` raised from SQLite's 1000-page default to 8192 pages with operator override via `TraceStore(wal_autocheckpoint_pages=…)`. Schema version stays at `1` (all changes additive); backup/restore continues to work across Wave-12 and Wave-13 DBs.
- **Pattern-store production-readiness audit (Wave 13, 13a-4)** — [`pattern-store.md §17`](docs/specs/pattern-store.md) "Production tuning" covers K-NN latency curve, embedding-cache throughput collapse at cap, concurrent-recording defense-in-depth lock, retention coordination with `trace-retention.md`, audit-flag posture confirmation. `PatternStore` ([`packages/metis/src/metis/core/patterns/store.py`](packages/metis/src/metis/core/patterns/store.py)) gains a `threading.RLock()` wrapping every public method; eliminates the `sqlite3.InterfaceError: bad parameter or other API misuse` failure mode under hostile thread contention (~36% failure rate at 100 threads × 10 `record()` calls; verified zero failures post-lock; uncontended under the documented single-asyncio-task architecture). New cache observability counters drive three Prometheus gauges per workspace: `metis_pattern_embedding_cache_hit_ratio` / `_hits_total` / `_misses_total`.
- **Buyer-trial quickstart + `metis trial` CLI (Wave 12b)** — [`docs/operations/quickstart.md`](docs/operations/quickstart.md) (~190 lines) ships the end-to-end < 1 hour buyer-trial path: kind cluster + helm install + `metis trial` against a pre-baked workload + per-key cost rollup via `/analytics/by_key`. Convenience scripts: [`infra/gateway/scripts/quickstart.sh`](infra/gateway/scripts/quickstart.sh) (idempotent automation; writes `.metis-trial/state.env`) + [`infra/gateway/scripts/tear-down.sh`](infra/gateway/scripts/tear-down.sh) (symmetric cleanup). Pre-baked workload at [`benchmarks/workloads-trial/refactor-extract-helper/`](benchmarks/workloads-trial/refactor-extract-helper/) — 3 turns, hybrid evaluator, `max_total_cost_usd: 0.10`, runs in < 2 minutes against haiku. New `metis trial --workload <name> --model <id> [--baseline <id>] [--db-path] [--gateway-url] [--gateway-key]` CLI ([`packages/metis/src/metis/cli/trial.py`](packages/metis/src/metis/cli/trial.py)) drives the workload end-to-end and prints a buyer-facing `actual / baseline / savings_pct / quality / cost-per-quality` block.
- **Partial-credit rubric primitive (Wave 14, 14a-1)** — [`evaluator.md §5.4`](docs/specs/evaluator.md) v1.1 → v1.2 lands the optional `partial_credit` block on `WorkloadRubric` and the parser in [`packages/metis/src/metis/core/eval/rubric.py`](packages/metis/src/metis/core/eval/rubric.py) / [`judge.py`](packages/metis/src/metis/core/eval/judge.py). When `enabled: true` + `criterion: test_pass_count_ratio`, the `HeuristicJudge._evaluate_workload` path bypasses the pass/fail substring assertion and reads `PASS N/M` / `FAIL N/M` runner output plus pytest summary tokens (`N passed`, `M failed`, `K error(s)`; skipped excluded from denominator); composition is `(base + partial_credit_score) / 2` so perfect-pass recovers `substring_present=True` and zero-pass recovers `substring_present=False`. `WORKLOAD_HEURISTIC_RUBRIC_VERSION` bumps `1.1.0 → 1.2.0` so dashboards distinguish the two score series per evaluator.md §12 invariant 7. Five workloads opt in (`regex-with-edge-cases` / `recursive-data-structure-traversal` / `subtle-bug-fix-with-test` / `refactor-with-contract-preservation` / `multi-file-refactor-with-shared-types`); the remaining 6 are unchanged because they don't carry a countable test output. 21 new tests in `tests/eval/test_judge.py` + `test_rubric.py`. **§A3-rev7 readout (partial):** the rubric works as designed end-to-end (`rubric_version=1.2.0` stamped on every workload verdict, mid-scores 0.63-0.75 produced on `regex-with-edge-cases` haiku Pass A) but on the 2 workloads with complete haiku + sonnet data the gap is exactly +0.000 — direct evidence for §A3-rev6 interpretation (a) that haiku-4.5 is genuinely strong on dev-loop coding at temperature=0.
- **Gateway self-serve signup endpoint (Wave 14, 14a-2)** — [`gateway.md §12`](docs/specs/gateway.md) lands as a new section; the route bundle is off by default. New module [`packages/metis/src/metis/gateway/signup.py`](packages/metis/src/metis/gateway/signup.py) ships `POST /signup` (email + workspace_name + optional `user_id` / `team_id` → magic-link verification URL, stdout in v1, SES/SendGrid deferred to Wave 15), `POST /signup/verify` (magic link → session token + first gateway key), `GET/POST /account/keys` (list / issue subsequent keys), `DELETE /account/keys/{id}` (revoke; wraps `keystore_admin.revoke_key`). Magic-link + session tokens are 32-byte URL-safe randoms with SHA-256-hashed persistence; magic links are single-use, 30-min TTL; session tokens 24-h TTL. `AccountStore` persists to `~/.metis/gateway/accounts.json` mode 0o600 via atomic write-temp-then-rename. Opt-in via `GatewayConfig.signup: SignupConfig | None` + `--enable-signup` flag; helm values gain `signup.{enabled,dashboardUrl,accountsPath}` block (default `enabled: false`). 20 new tests in [`packages/metis/tests/gateway/test_signup.py`](packages/metis/tests/gateway/test_signup.py). Issued keys participate in the same `gateway.key_issued` / `gateway.key_revoked` audit events and `/analytics/by_key` rollups — no event-catalog change.
- **mkdocs-material doc site scaffolding (Wave 14)** — [`mkdocs.yml`](mkdocs.yml) at repo root + [`docs/index.md`](docs/index.md) + [`infra/docs/Dockerfile`](infra/docs/Dockerfile) + `docker-compose.yml` `docs` profile. Material theme, full-text search, per-page GitHub edit links, light/dark palette toggle, `pymdownx.*` extensions. Four top-level sections in `nav:` (Getting Started / Specs / Operations / Reference). `mkdocs build --strict` validates internal links; pre-existing spec→source-code links (`../../packages/metis-core/...`) downgraded to `info` so they don't trip strict mode while nav-level issues stay at `warn`. The image mirrors the gateway-image shape (same uid/gid, env-driven entrypoint, healthcheck cadence); docker-compose `docs` profile means `docker compose up` (no profile) still runs gateway-only. Local preview: `uv run --with mkdocs-material mkdocs serve`. The full doc tree is pure-static once built — any static host works for production.
- **Production-grade observability extensions (Wave 14a)** — [`observability.md`](docs/specs/observability.md) bumps `v1 → v1.1` with six additive metrics ([`packages/metis/src/metis/core/observability/metrics.py`](packages/metis/src/metis/core/observability/metrics.py)): `metis_routing_decision_latency_seconds` (histogram, 100µs-500ms buckets, off `route.decided.elapsed_ms`), `metis_tool_call_latency_seconds{tool_name}` (histogram, 5ms-30s buckets, off both `tool.completed` + `tool.failed` with `tool_name` correlated via a bounded in-collector LRU since the event schema doesn't carry it on completed/failed), `metis_llm_call_errors_total{provider,model,error_class}` (counter — failure-only series distinct from the legacy mixed `metis_llm_calls_total{status}` so rate() queries don't sum across status labels), `metis_tool_failures_total{tool_name,error_class}`, `metis_gateway_auth_failures_total{reason}` (counter, off the new `gateway.auth_failed` audit-flagged event with 3-value `GatewayAuthFailureReason` enum: `missing_token` / `invalid_token` / `key_revoked`), and `metis_gateway_key_cost_usd_total{gateway_key_id}` (counter — agent-loop traffic without a key collapses under `gateway_key_id="null"` per the multi-user.md §3.4 null-bucket convention). The verified LLM-latency-bucket range (`_LATENCY_BUCKETS_SECONDS` 50ms-120s) already spans the spec's 0.1s-30s alert-target. New `gateway.auth_failed` event payload (`{reason, inbound_shape, token_hash_prefix, gateway_key_id}`, PSEUDONYMOUS, audit-flagged) emitted from both inbound shapes (OpenAI + Anthropic) at all three rejection points; raw token never on the bus, SHA-256 first 8 hex chars persisted so SIEM can correlate repeated leaked-credential attempts. Helm chart: [`infra/gateway/helm/templates/prometheus-rules.yaml`](infra/gateway/helm/templates/prometheus-rules.yaml) renders a `monitoring.coreos.com/v1.PrometheusRule` with four alert templates (`MetisLLMCallLatencyP99High` p99 > 30s / 5m, `MetisLLMErrorRateHigh` > 5% / 10m, `MetisGatewayAuthFailureRateHigh` > 0.1/sec / 5m, `MetisGatewayKeyCostSpike` > $10/hr/key / 10m), each independently tunable in `values.yaml` under `monitoring.prometheusRules.<rule>` and gated on `monitoring.prometheusRules.enabled: false` default; [`infra/gateway/helm/dashboards/metis-gateway.json`](infra/gateway/helm/dashboards/metis-gateway.json) ships a 13-panel Grafana dashboard (5 rows) for buyer import. [`docs/operations/observability-runbook.md`](docs/operations/observability-runbook.md) (~340 lines) pairs each alert with PromQL + first-action checklist + mitigations + false-positive patterns + a one-week tuning workflow. 17 new tests across all three suites (9 in [`packages/metis/tests/core/observability/test_metrics.py`](packages/metis/tests/core/observability/test_metrics.py) + 3 in [`packages/metis/tests/gateway/test_metrics_endpoint_gateway.py`](packages/metis/tests/gateway/test_metrics_endpoint_gateway.py) + 3 in [`packages/metis/tests/server/test_metrics_endpoint_server.py`](packages/metis/tests/server/test_metrics_endpoint_server.py)); helm chart lints clean with the new template enabled.
- **NETWORK error refinement (Wave 15, 15a-1)** — closes GA blocker 1 from the Wave-14 GA-readiness audit. [`routing-engine.md §4.5.1`](docs/specs/routing-engine.md) refines the NETWORK trigger: a single transient SSL / DNS error no longer blacks out the whole provider — `_NETWORK_PROVIDER_ESCALATION_THRESHOLD = 2` within a `_NETWORK_PROVIDER_ESCALATION_WINDOW_SECONDS = 30.0` sliding window is required to escalate provider-wide, and a single failure still advances the per-(provider, model) 5-within-2-min counter so a persistently broken model still trips itself. `_PROVIDER_WIDE_IMMEDIATE_CLASSES` is removed; AUTH remains an immediate provider-wide escalation via a direct `error_class == ErrorClass.AUTH` check. `_ProviderState.recent_network_failures: list[float]` is the sliding window ([`packages/metis/src/metis/core/routing/availability.py`](packages/metis/src/metis/core/routing/availability.py)); `mark_success` / `force_recovery` / the auto-recovery path in `state()` all clear it alongside `recent_model_unavailables`. Refinement — not a contract change: `validation_failure` strings unchanged, `route.decided.chain` shape unchanged, event-catalog payload registry unchanged; the only observable difference is that one transient SSL error no longer flips the whole-provider scope. 7 new tests replace 2 stale ones in [`tests/routing/test_availability.py`](packages/metis/tests/core/routing/test_availability.py) (single NETWORK doesn't blackout, 2-within-30s does, 2-at-31s-apart doesn't, model=None still escalates, single NETWORK still advances per-(provider, model) counter, `mark_success` clears window, AUTH still escalates immediately). The audit's sub-issues 2 (no retry inside `metis trial`) and 3 (wrong-model error message) remain open — both outside the availability state machine.
- **Gateway model normalization (Wave 15, 15a-2)** — closes GA blocker 2 from the Wave-14 GA-readiness audit. New `_normalize_inbound_model(name, *, inbound_shape, registry)` helper in [`packages/metis/src/metis/gateway/harness.py`](packages/metis/src/metis/gateway/harness.py) runs once per request, immediately before `registry.resolve_alias`, in both `call()` and `stream()` paths. Bare provider model names from SDK clients (Anthropic SDK: `claude-3-5-haiku-20241022`; OpenAI SDK: `gpt-4o-mini`) now get the canonical provider prefix prepended before the routing engine sees them — slot 1 (`per_message_override`) resolves to the canonical id, pricing bills under the actual model, and the chain no longer falls through to slot 7 `global_default = sonnet` with a ~6× cost over-report. Rules in order: (1) registry-known alias or canonical id → pass through; (2) `metis://...` opt-out → pass through; (3) already-prefixed `provider:name` → pass through; (4) bare name → prepend `{"openai":"openai","anthropic":"anthropic"}[inbound_shape]`. Outbound JSON body still echoes the client's raw `model` string verbatim — SDKs that compare echo-against-sent continue to work; existing fallthrough tests (`test_chat_completions_falls_back_to_global_default`, `test_messages_accepts_x_api_key_header`) are unchanged and still pass. Translators stay pure — no registry dependency added. [`gateway.md §4.8`](docs/specs/gateway.md) documents the new normalization step; §5.3 step 3 points at §4.8 for bare-name handling. 9 new tests in [`packages/metis/tests/gateway/test_model_normalization.py`](packages/metis/tests/gateway/test_model_normalization.py) — 6 unit tests on the helper + 3 end-to-end via the scripted-adapter fixture asserting (i) the adapter receives the canonical `provider:name`, (ii) `route.decided.chosen_model` records the canonical id, (iii) `llm.call_completed.cost_usd` matches the haiku / gpt-4o-mini per-token rate.
- **Concierge onboarding tooling (Wave 15, 15a-3)** — Buyer-trial → conversion CLI surface for the first paid Metis customer. Two new CLI subcommands: `metis customer-report --workspace <path> --since <date> [--out path] [--format html|json] [--customer-label …] [--customer-tier trial|paid|internal] [--baseline …]` ([`packages/metis/src/metis/cli/customer_report.py`](packages/metis/src/metis/cli/customer_report.py)) renders an offline-share-able HTML report (no JS, no external assets, inline CSS only — browser-print-to-PDF for archival) or deterministic JSON with spend / savings_pct / cost-per-quality stat tiles + by_model / by_gateway_key / by_user / by_team / daily_spend tables, HTML-escaping every customer-controlled string; `metis trial-status <workspace> [--db-path] [--since] [--trial-length-days 7] [--baseline]` ([`packages/metis/src/metis/cli/trial_status.py`](packages/metis/src/metis/cli/trial_status.py)) reports spend / quality / days-into-trial and a 0-100 conversion-readiness band (`ready` ≥ 80 / `warm` 50-79 / `not_yet` 1-49 / `no_signal` 0) derived from three coarse axes (usage signal: calls + spend; quality signal: verdict count + mean score; trial progress: days-in vs trial-length). Threshold constants (`MIN_SPEND_FOR_SIGNAL_USD=0.50`, `MIN_LLM_CALLS_FOR_SIGNAL=20`, `MIN_QUALITY_VERDICTS=5`, `HEALTHY_QUALITY_FLOOR=0.70`) pinned by test. Keystore gains an optional `customer_tier: Literal["trial","paid","internal"] | None` field on `GatewayKey` + the `GatewayKeyIssued` audit payload — **additive, NOT an entitlement gate**; the gateway does NOT read tier for auth / routing / rate-limit / quota decisions. `metis gateway issue-key --customer-tier <tier>` persists it under the same omit-when-`None` convention as `user_id` / `team_id`. 29 new tests (13 customer-report + 12 trial-status + 4 issue-key tier extension). Operator-facing concierge playbook lives in the private `metis-pro` repo.
- **Status-page live deployment recipe (Wave 15, 15a-4)** — [`docs/operations/status-page.md`](docs/operations/status-page.md) gains three new top-level sections: (1) "Live deployment" — honest about the hosting-account split (helm toggle + curl recipes are ready-to-apply; DNS / TLS / SaaS-account provisioning is owner-side; `https://status.example.com` is the target hostname already referenced by the Wave-14 product-site nav + footer); (2) "Monitoring checks" — four canonical probes with full field-by-field configuration: `/healthz` HTTP liveness (60s interval, 2-fail → SEV1), synthetic `POST /v1/messages` with a dedicated capped key (`--daily-cap-usd 0.50 --allow-model anthropic:claude-haiku-4-5`; ~$1/mo at 5-min cadence), `/metrics` HTTP-keyword on `metis_gateway_keys_active` (catches the "bus has stalled" failure mode `/healthz` misses), gateway-key liveness via Kuma Push + curl-cron correlating `metis_gateway_keys_active` against `gateway.key_rotated` audit events; (3) "Severity-mapped templates" — the existing stage templates pre-instantiated for SEV1 / SEV2 / SEV3 / SEV4 quoting [`incident-response.md §Severity levels`](docs/operations/incident-response.md) verbatim. New helm template [`infra/gateway/helm/templates/statuspage.yaml`](infra/gateway/helm/templates/statuspage.yaml) (~140 lines, gated on `statusPage.enabled: false` default) renders PVC + Deployment + Service + optional Ingress for an Uptime Kuma sidecar; five new helpers in [`_helpers.tpl`](infra/gateway/helm/templates/_helpers.tpl) (the status-page resources carry `app.kubernetes.io/name: <chart>-status-page` so the gateway Service selector can't accidentally match the Kuma pod); `strategy.type: Recreate` so two Kuma writers don't overlap on SQLite. Helm chart lints clean; pre-Wave-15 deployments byte-identical. **The deploy step is NOT executed** (no UptimeRobot account, no DNS for `status.example.com`) — those five owner-side steps are enumerated in §Live deployment > Provisioning checklist.
- **Billing module (Wave 15, 15a-7)** — Owner ratified [`pricing.md §5.5.4`](docs/specs/pricing.md) (open-core gateway + per-seat Pro + reserved enterprise %-of-savings add-on) on 2026-05-16. New module [`packages/metis/src/metis/gateway/billing/`](packages/metis/src/metis/gateway/billing/): `client.py` exposes a thin `BillingClient` Protocol with `StripeBillingClient` (real `stripe.Customer.create` / `stripe.Subscription.create` / `stripe.SubscriptionItem.create_usage_record` / `stripe.PaymentMethod.attach` / `stripe.Webhook.construct_event`) + `FakeBillingClient` (in-memory event log; test substrate); `store.py` persists `customer_records` + `subscription_records` + `processed_events` (webhook idempotency) to `~/.metis/gateway/billing.db`; `subscriptions.py` orchestrates the per-account tier-transition state machine (`create_pro_subscription(account_id, seats)`, `attach_enterprise_addon(account_id, savings_rate_pct, monthly_cap_usd)`, `record_savings_usage(account_id, savings_usd)` keyed on cents-of-savings recouped this billing cycle); `webhooks.py` handles `customer.subscription.updated` / `customer.subscription.deleted` / `invoice.payment_succeeded` / `invoice.payment_failed` with `stripe.Webhook.construct_event` signature verification + processed-event dedupe; `routes.py` mounts `/account/billing/*` (subscription view, payment-method update, cancel-or-pause) + `/webhooks/stripe`. `QuotaConfig.tier: Literal["free","pro","enterprise"]` + `tier_overrides` compose with the existing per-key / per-user / per-team / per-workspace caps (free: `daily_cap_usd=0.17` / `monthly_cap_usd=5.0`; pro / enterprise unlimited at the tier level — still bounded by the per-(user/team/key/workspace) caps). Six new audit-flagged event types in [`event-bus-and-trace-catalog.md §6.14`](docs/specs/event-bus-and-trace-catalog.md): `billing.customer_created` / `billing.subscription_created` / `billing.subscription_updated` / `billing.subscription_canceled` / `billing.invoice_paid` / `billing.invoice_payment_failed` — all PSEUDONYMOUS (carry `account_id`, not `email`); all added to `AUDIT_EVENT_TYPES`. New CLI subcommand group: `metis billing status --account-id <id>` (operator view) / `metis billing usage-record --account-id <id> --savings-usd <amount>` (manual metered post) / `metis billing sweep-usage` (recurring auto-post). Opt-in: `GatewayConfig.billing: BillingConfig | None = None`; mounting requires `--enable-billing` (or helm `billing.enabled: true`). Pre-Wave-15 deployments byte-identical. `stripe>=9` is an optional `[billing]` pyproject extra; `FakeBillingClient` lets the test suite run without it. 38 new tests in [`packages/metis/tests/gateway/test_billing/`](packages/metis/tests/gateway/test_billing/). Stripe live-mode validation deferred to first-buyer onboarding — Wave 15 uses test mode exclusively.
- **Billing self-service UX (Wave 16, 16a-4)** — [`packages/metis/src/metis/gateway/billing/routes.py`](packages/metis/src/metis/gateway/billing/routes.py) adds `GET /account/billing/portal` (Stripe Customer Portal session) and `POST /account/billing/plan` (`free` / `pro` / `enterprise` self-service plan changes). [`BillingService`](packages/metis/src/metis/gateway/billing/subscriptions.py) now handles enterprise add-on attach/remove, deterministic Decimal metered-usage rounding, failed-payment grace (`failed_payment_grace_days=7`), post-grace downgrade to Free-tier cap enforcement, and payment-succeeded restoration. Operator-facing refund/dispute/manual-change runbook lives in the private `metis-pro` repo. 57 billing tests cover the FakeBillingClient substrate, portal URL, plan transitions, failed-payment grace/freeze/restore, and free-tier cap summary.
- **First-customer concierge artifacts (Wave 16, 16a-2)** — [`benchmarks/customers/`](benchmarks/customers/) now carries gitignored customer data scaffolding plus tracked intake/report/workload templates; actual `<customer-id>/before`, `<customer-id>/after`, and generated `report.md` artifacts stay ignored. `metis customer-report --anonymize` pseudonymizes customer labels, workspace paths, trace DB paths, gateway keys, users, and teams deterministically for shareable reports. 17 customer-report CLI tests cover deterministic anonymization and placeholder substitution.
- **Operational readiness docs (Wave 16, 16a-6)** — [`docs/operations/status-page-config.yaml`](docs/operations/status-page-config.yaml) provides paste-ready status-page probe configuration when hosting is not yet provisioned. Launch-day playbook, pre-launch dry-run checklist, and support-channel templates live in the private `metis-pro` repo.
- **Wave 14 docs sync** — AGENTS.md + README.md test counts bumped 1678 → 1722; strategy doc picks up the §A3-rev7 partial finding (interpretation (a) directly evidenced on 2 workloads, interpretation (c) largely refuted on those same 2 workloads, residual signal on `regex-with-edge-cases`) and the GTM-ordering pivot (delegation now ahead of model-selection).
- **Wave 15 docs sync** — AGENTS.md + README.md test counts bumped 1722 → 1829; AGENTS.md status sentence extended through the Wave-15 batch (GA-blocker-1 NETWORK refinement + GA-blocker-2 model normalization + concierge tools + billing module + status-page live-deployment recipe); five new "What works" entries land between the Wave-14a observability bullet and the Wave-14-docs-sync bullet. Delegation gets a third independent datapoint at 19.9% cost-per-quality (8.3% / 19.9% / 26.1% across three runs).
- **Wave 16 docs sync / GA milestone** — Owner ratified Position B on 2026-05-16: Phase 3 shipped, no "Phase 4 v1 started" claim. AGENTS.md + README.md status mirrors now say Phase 3 shipped and GA launch milestone reached. The optional §A3 task-domain wedge is explicitly deferred post-GA; delegation remains the validated routing-surface GTM lever. Test count refreshed 1829 → 1771.

**What's NOT built (next-up):**
- **Context-assembler v3 skill activation** — spec is shipped (`context-assembler.md §5.2`) and partial scaffolding exists (`_assemble_stable_system_prompt` / `_initialize_skill_activations` / `SkillActivationRegistry`); the agent-side activate-on-tool-use path and the explicit-activation budget aren't fully wired yet.
- **Skill format loader extensions** — agentskills.io-compatible on-disk skill packaging beyond the current `SkillStore` substrate. Spec drafted at `docs/specs/skill-format.md`; loader extensions deferred.
- **Skill curator** — periodic auxiliary-model maintenance of agent-authored skills (pin / archive / consolidate / edit); never auto-deletes, archive is reversible, pinned bypasses auto-transitions; shares the evaluator's `BudgetTracker` primitive with independent caps. Pattern lifted from hermes-agent. Spec drafted at `docs/specs/skill-curator.md`; implementation gated on Phase 2.5 agent-authored skills (`skill_save` + `skill.created(source="auto_generated")`) landing first.
- **Delegation v1 follow-ons** — async/concurrent workers, cancellation cascade from planner to in-flight workers, streaming worker output back to the planner, recursive (worker-spawns-worker) delegation, `output_schema` validation, per-tier worker timeout, router-decided delegation (slot 5 as the entry point, not just inside worker re-entry), worker pattern-store integration. See `delegation.md §3.6` for the canonical deferral list.
- **Pattern store v2 cluster-tightening A/B against a real-API fixture** — pattern-store.md §16.10 test 5 lands in Wave 11 as a synthetic fixture in `test_v2_cluster_tightening.py` (passes the ≥0.10 intra / ≥0.05 inter gates with comfortable margin). The remaining "real fixture" version — running the same gates against vectors produced by `openai:text-embedding-3-small` or `local:sentence-transformers:all-MiniLM-L6-v2` over actual `§A3-rev2`/`§A3-rev4` patterns DB user messages — is deferred. The synthetic fixture is the load-bearing gate (it controls the cluster geometry exactly); the real-API version is a confidence check that real embedders preserve the cluster signal.
- **§A3 task-domain wedge** — the next model-selection research move is outside the dev-loop suite: math/symbolic, long-context multi-document synthesis, and rare API surfaces where haiku-vs-sonnet outcome distance may be large enough to overcome sonnet variance. Deferred post-GA; do not spend live API budget on it during launch operations unless the owner explicitly re-prioritizes it.

## Read before reasoning about the system

The design is specified before code lands. In order of load-bearing-ness:

1. [docs/project-overview.md](docs/project-overview.md) — vision, principles, architecture, phasing.
2. [docs/specs/canonical-message-format.md](docs/specs/canonical-message-format.md) — the foundational data contract; every other spec depends on it.
3. [docs/specs/event-bus-and-trace-catalog.md](docs/specs/event-bus-and-trace-catalog.md) — observability spine, closed event catalog.
4. [docs/specs/routing-engine.md](docs/specs/routing-engine.md) — model selection pipeline, delegation.
5. [docs/specs/streaming-protocol.md](docs/specs/streaming-protocol.md) — WebSocket protocol (v1 subset implemented; see "What's NOT built" for gaps).
6. [docs/specs/provider-adapter-contract.md](docs/specs/provider-adapter-contract.md), [docs/specs/tool-dispatcher.md](docs/specs/tool-dispatcher.md), [docs/specs/server-api.md](docs/specs/server-api.md) — component contracts.
7. [docs/specs/analytics-api.md](docs/specs/analytics-api.md), [docs/specs/context-assembler.md](docs/specs/context-assembler.md), [docs/specs/benchmark.md](docs/specs/benchmark.md) — savings-attribution surface, prompt-cache placement, and the workload suite that turns the dashboard's numbers into a credible "we saved X%."
8. [docs/specs/deployment-shape.md](docs/specs/deployment-shape.md), [docs/specs/gateway.md](docs/specs/gateway.md), [docs/specs/pattern-store.md](docs/specs/pattern-store.md), [docs/specs/evaluator.md](docs/specs/evaluator.md), [docs/specs/skill-format.md](docs/specs/skill-format.md), [docs/specs/skill-curator.md](docs/specs/skill-curator.md) — deployment-shape, gateway, pattern-store, and evaluator are shipped; skill-format has the current substrate plus deferred loader extensions; skill-curator is drafted but gated on agent-authored skills landing first.
9. [docs/specs/CHANGES.md](docs/specs/CHANGES.md) — cross-spec change log; check `pending review` entries before editing dependent specs.

For competitive landscape and prior art: [docs/market-research/synthesis.md](docs/market-research/synthesis.md) and the four per-stream reports alongside it.

## Architecture and package layering

The repo is a uv workspace with one published package `metis` ([`packages/metis/`](packages/metis/)), organized internally into four subpackages:

```
metis.core      (packages/metis/src/metis/core/)
metis.server    (packages/metis/src/metis/server/)        depends on metis.core
metis.gateway   (packages/metis/src/metis/gateway/)       depends on metis.core
metis.cli       (packages/metis/src/metis/cli/)           depends on metis.core, metis.server, metis.gateway
```

`metis.server` and `metis.gateway` are independent siblings — neither imports the other. Both consume `metis.core` building blocks (registry, routing engine, adapters, pricing, event bus, trace store) and expose their own Starlette ASGI surface. `metis.server` is the agent-mode surface (sessions, tools, memory, streaming); `metis.gateway` is the transparent-proxy surface (per-request stateless, no agent loop).

Within `metis.core`, the internal layering is (lower can be imported by higher, never the other way):

```
canonical      ←  events, trace, tools, adapters, routing, pricing, memory, patterns, eval, sessions
events         ←  trace, tools, adapters, routing, patterns, eval, sessions
trace          ←  eval
adapters       ←  pricing, sessions
tools          ←  memory, sessions
memory         ←  sessions
patterns       ←  routing, sessions
routing        ←  sessions
pricing        ←  sessions
eval           ←  (consumed by metis.cli only — bus subscriber + `metis evaluate` CLI)
sessions       ←  (nothing else in core)
```

`canonical` is the foundation; anything can import from it. Nothing in `canonical` imports from any other module. `metis.cli` is the top — it composes a `ChatRuntime` that the REPL, TUI, and `serve` subcommand all consume. `metis.server` sits just below `metis.cli`: it accepts a `ChatRuntime` (built by `metis.cli.runtime`) and exposes it over HTTP/WebSocket. When adding code, respect this direction — a circular import means a missing abstraction.

## Working norms

- **Specs-first.** Don't propose implementation changes that contradict the specs without flagging the spec impact. If a spec needs to change, draft the spec change first, then the code.
- **Cross-spec discipline.** When a spec change touches a contract, add an entry to `docs/specs/CHANGES.md` (date, change, type, references to verify, status). See the file's header for the format.
- **Solo, part-time owner.** One engineer, ~part-time pace. Scope decisions should favor what one person can land and maintain, not what a team could.
- **Bounded memory is a feature.** `MEMORY.md` and `USER.md` are intentionally small (~2 KB / ~1.5 KB). Don't propose unbounded growth — the eviction is the point.
- **This repo is OSS (Apache-2.0).** See [`LICENSE`](LICENSE) and [`CONTRIBUTING.md`](CONTRIBUTING.md). A private companion repo `metis-pro` (at `~/git/metis-pro/`) holds the paid-tier overlay — billing, signup, accounts, hosted dashboard UI, curated LLM-judge rubric library, enterprise SAML/OIDC/SCIM — plus the operator-facing runbooks (concierge onboarding, first-customer cadence, launch-day playbook, billing-operator guide, support channels), the sales toolkit (one-pager, FAQ, competitive comparison, objection handling, case-study templates), and the strategy / business-model docs. The two repos integrate via five extension Protocols: four in [`packages/metis/src/metis/core/extensions.py`](packages/metis/src/metis/core/extensions.py) (`BillingBackend`, `SignupBackend`, `AnalyticsExtension`, `JudgeRubricProvider`) plus the gateway-typed [`TierCapsResolver`](packages/metis/src/metis/gateway/extensions.py). **The OSS substrate must stay standalone-usable end-to-end** with noop default implementations. The pricing-model decision is recorded in [`docs/specs/pricing.md §12`](docs/specs/pricing.md).

### Working across both repos

If a change touches both repos, the discipline is:

1. **OSS first, always.** Add the Protocol method (or new event payload, or new HTTP route) here, with noop default + extension-contract test. Merge.
2. **Wait for PyPI release** of `metis`.
3. **Bump pin in `metis-pro/pyproject.toml`** (`metis>=X`) and implement against the new contract.
4. Never the reverse — `metis-pro` cannot consume a Protocol method that doesn't exist in the released OSS yet.

For Claude Code sessions: single-repo work runs from the relevant repo's directory and inherits its `AGENTS.md`. Cross-repo work either runs as two sequenced sessions (cleaner) or one session with absolute paths and explicit `git -C <repo> …` for each commit. See [`metis-pro/AGENTS.md`](../metis-pro/AGENTS.md) for the Pro repo's conventions.

## Implementation conventions

- **`msgspec.Struct(frozen=True)`, not Pydantic.** Canonical types use msgspec with tagged unions for discriminated content. See [canonical/content.py](packages/metis/src/metis/core/canonical/content.py).
- **`next_monotonic_ulid()`** from [canonical/ids.py](packages/metis/src/metis/core/canonical/ids.py). Raw `ULID()` from `python-ulid` is NOT strictly monotonic within a millisecond; the spec requires monotonic, so we wrap it with a process-wide lock that bumps the integer value on tie. Any new id generator must use it.
- **Tool factories, not instances.** `ToolDispatcher.register(ReadFileTool)` — pass the class. The dispatcher instantiates a fresh tool per call to prevent shared state across concurrent dispatches. The test suite enforces this.
- **Typed event payloads + dict envelope.** Each event type has a typed `msgspec.Struct` payload in [events/payloads.py](packages/metis/src/metis/core/events/payloads.py); `Event.payload` is `dict`. Use `make_event(type=..., payload=TypedStruct(...))` to bridge — it validates the type↔payload binding and converts.
- **`CanonicalResponse` returns content, not Message.** The adapter doesn't know the routing decision or the cost; the caller (`SessionManager`) assembles the full `Message` with metadata. This is a documented deviation from `provider-adapter-contract.md` §3.3 — see the file docstring at [adapters/protocol.py](packages/metis/src/metis/core/adapters/protocol.py).
- **Async fixtures.** Test fixtures that call `bus.start()` (which uses `asyncio.create_task`) must be `async def` so they run with an event loop. See [packages/metis/tests/core/tools/test_dispatcher.py](packages/metis/tests/core/tools/test_dispatcher.py).
- **Cost is `Decimal`, not float.** `Usage.cost_usd` and `PriceTable.compute_cost` use `Decimal` to avoid drift on cent-level math. `pricing_version` is recorded with every cost record so historical traces can be re-priced.
- **Workspace path security.** Any tool that touches files goes through `WorkspaceFileAPI`. `..` segments resolve during checking; symlinks pointing outside the root are rejected. Do not bypass it.

## Adding a new X

**New event type:**

1. Define a `msgspec.Struct(frozen=True)` payload in [packages/metis/src/metis/core/events/payloads.py](packages/metis/src/metis/core/events/payloads.py).
2. Add `"my.event": (MyPayload, Sensitivity.PSEUDONYMOUS)` to `PAYLOAD_REGISTRY`.
3. Update `docs/specs/event-bus-and-trace-catalog.md` §6 with the payload schema and sensitivity.
4. Log to `docs/specs/CHANGES.md` (additive vs breaking; which other specs to verify).
5. Tests cover the round-trip and the catalog registry membership.

**New built-in tool:**

1. Implement the `Tool` protocol from [tools/protocol.py](packages/metis/src/metis/core/tools/protocol.py) — `definition: ToolDefinition`, `async execute(input, context)`, `async cancel()`.
2. Schema must conform to the JSON Schema subset in [canonical/tools.py](packages/metis/src/metis/core/canonical/tools.py) — no `$ref`, `oneOf`, `anyOf`, `allOf`, `not`, etc.
3. Workspace-scoped file ops go through `context.workspace_files: WorkspaceFileAPI`.
4. Register via `register_builtins()` in [tools/builtins/__init__.py](packages/metis/src/metis/core/tools/builtins/__init__.py) if it's a default tool.

**New provider adapter:**

1. Implement the `ProviderAdapter` Protocol from [adapters/protocol.py](packages/metis/src/metis/core/adapters/protocol.py).
2. Wire translation: see [adapters/anthropic.py](packages/metis/src/metis/core/adapters/anthropic.py) for the pattern — system hoist, TOOL→user-with-tool_result merge, tool id map. `ToolIdMap` becomes load-bearing when providers don't accept canonical ids verbatim (OpenAI's `call_*` ids).
3. Error classification: implement a `classify_<provider>_response()` and return via `error_for_class()` with the correct `ErrorClass`.
4. Declare per-model `AdapterCapabilities` honestly (false ≥ unsupported); routing's validation gate trusts these.
5. Register via `ModelRegistry.register(model_id=..., adapter=..., aliases=...)`.

## Gotchas (things that will surprise you)

- **Streaming events are a separate layer**, not bus catalog events. `message.start`, `text.delta`, `tool.use_*`, `message.complete` exist in `streaming-protocol.md §5.3` but are NOT in `PAYLOAD_REGISTRY`. The streaming server has two input channels: the bus bridge (catalog events, persisted) via `EventBus.subscribe`, and a direct channel from the agent loop via [server/hub.py](packages/metis/src/metis/server/hub.py) (streaming events, transient). The trace store only sees the former.
- **Turn-locked model.** The model chosen at turn start owns every LLM call in the turn, including tool cycles. Routing only re-runs at turn boundaries. Don't add code paths that re-route mid-turn — it breaks cost predictability and the `route.decided` invariant.
- **Stub policy** (`delegate_request`) still appears in `route.decided.chain` with `verdict: "not_applicable"`. That's correct, not a bug — it'll be filled in in Phase 4. The `rule` slot parses `.metis/routing.yaml`; the `pattern` slot consults the per-workspace `PatternStore` when a resolver is injected (otherwise reports `not_applicable` with reason `"pattern store not configured"`).
- **Per-(provider, model) availability.** A 401 or capability mismatch on one model no longer marks the whole provider unavailable; routing tracks availability per `(provider, model)` pair. The routing-engine.md §11 deferred-behavior note is resolved.
- **Memory is per-session, not per-process.** Each `SessionManager.create_session` builds a fresh `MemoryStore` via the injected `memory_factory`. The on-disk files (`<workspace>/.metis/{MEMORY.md,USER.md}`) are shared across sessions in the same workspace, but each session's store reads them fresh.
- **Memory writes don't auto-truncate.** Soft-cap overflow emits `memory.eviction` as a signal; hard-cap overflow rejects the write so the agent has to `memory_consolidate`. The eviction is the spec's intended user-visible action, not silent garbage collection.
- **`CLIConfirmationHandler` is the default confirmation handler** for `metis chat` and `metis tui`. NONE/READ side effects auto-approve; WRITE/EXECUTE/NETWORK prompt at the terminal unless the tool name is in `<workspace>/.metis/trust.yaml`'s `always_allow` (the prompt's "always" answer appends there; "never" appends to `always_deny`). The prompt has a 60-second timeout; on timeout the call is denied. Pass `--auto-allow` to `metis chat` / `metis tui` to revert to the old `AutoAllowHandler` behavior. `metis serve` is unaffected — the server installs `RemoteConfirmationHandler` on top of whatever the CLI set.
- **PARTIAL message bypass.** `validate_message()` skips invariant checks when `metadata.status == PARTIAL` (canonical-format §5.1.5). Mid-stream messages are intentionally allowed to violate role-content rules.
- **`metis serve` binds loopback-only; `metis gateway` defaults loopback but accepts non-loopback opt-in.** `metis serve --host 0.0.0.0` is silently rewritten to `127.0.0.1` (server-api.md §3.1 — agent server retains loopback-only until its auth story lands). `metis gateway` defaults to `127.0.0.1` (back-compat) but **as of Wave 13 accepts `--host 0.0.0.0`** — the silent rewrite is gone. Non-loopback binds emit a one-time hardening WARN summarizing `tls_in_process=on|off rate_limit=on|off` and require the documented hardening checklist (rate limit + audit log + TLS termination per gateway-hardening.md §2-3). New CLI flags: `--tls-cert`, `--tls-key`, `--max-connections` (default 1000), `--reuse-port`. Helm values gain `gatewayHost`, `maxConnections`, `tls.{enabled,secretName,mountPath}`. Migration recipe at `docs/operations/upgrade-guide.md §6`.
- **Attach tokens are single-use, 60-second TTL.** Each `GET /sessions/{id}` mints fresh; the WebSocket consumes it on upgrade. Reconnects need a new HTTP roundtrip.
- **Gateway clients passing `model` in the request body win the routing chain at slot 1.** OpenAI / Anthropic SDKs always include `model`, so `route.decided.chain` reports `policy=per_message_override` with `verdict=chose` on every gateway request. The `rule`, `pattern`, `workspace_default`, and `global_default` slots are not exercised unless the client omits `model`. This is correct (gateway.md §V interprets the inbound `model` as a per-message override) but worth knowing when reading gateway traces — you won't see other slot wins until a client deliberately omits `model`.
- **Gateway is per-request stateless.** No session manager, no tool dispatcher, no memory store, no skill loader. A turn is one HTTP request. This is the documented gateway-mode boundary (gateway.md §2), not a TODO. Clients owning conversation history is the point.
- **Gateway self-serve signup is off by default.** `GatewayConfig.signup` is `None` and the `/signup` + `/account/keys` routes are unmounted unless `--enable-signup` (or helm `signup.enabled: true`) is set. Pre-Wave-14 deployments are byte-identical. When enabled, magic links are logged to stdout in v1 — Wave 15 wires SES/SendGrid. The accounts file (`~/.metis/gateway/accounts.json`, mode 0o600) stores plaintext email like multi-user.md's `users.json`; the trace store still never sees email, only `user_id`. Signup-issued keys are ordinary `GatewayKey` records — they participate in the same audit events and `/analytics/by_key` rollups.

## Non-obvious external context

- **LiteLLM is *not* safe as the canonical internal representation.** It has live bugs around `tool_use`, `cache_control`, and thinking-block translation. Write per-vendor adapters against canonical types; use LiteLLM only as an optional egress proxy if at all.
- **agentskills.io is a verified open standard** (Anthropic-originated, multiple implementers). When designing the skill format, conform to it; don't invent fields.
- **Letta is the bounded-memory peer worth studying** (Series A; ships core/archival/recall memory plus agent self-edit tools). Reference its mechanics before redesigning memory primitives.

## Stack and conventions

- **Python 3.13**, `uv` workspace (per-member `uv_build` backend; see each `pyproject.toml`).
- **`msgspec`** for canonical types (not Pydantic).
- **`anthropic`** SDK + `httpx` for the provider adapter.
- **`jsonschema`** (Draft 7) for tool input validation.
- **`python-ulid`** wrapped with `next_monotonic_ulid()` for monotonic per-process ids.
- **Ruff** lint+format: line length 100, rules `E,F,I,B,UP,RUF`, `E501` ignored. Avoid the `×` multiplication sign in strings (RUF001 flags it as ambiguous).
- **pytest** with `pytest-asyncio` in auto mode; each workspace member has its own `tests/` directory. Root `conftest.py` puts `tests_shared/` on `sys.path` so cross-member test helpers (e.g. the scripted adapter) are importable from anywhere.
- **mypy** for typing.

## Running things

```bash
# Sync the workspace (resolves all four members).
uv sync

# Tests (1771 currently — collected from all four workspace members including packages/metis/tests/gateway).
uv run pytest

# Lint + format
uv run ruff check packages apps scripts
uv run ruff format packages apps scripts

# CLI (requires at least one of ANTHROPIC_API_KEY / OPENAI_API_KEY / OPENROUTER_API_KEY)
uv run metis chat .
uv run metis chat /path/to/workspace --model haiku
uv run metis tui /path/to/workspace
uv run metis serve /path/to/workspace --port 8421

# Transparent gateway: issue a key (printed once), then run the server.
# Clients hit http://127.0.0.1:8422/v1/chat/completions (OpenAI) or /v1/messages (Anthropic).
uv run metis gateway issue-key --name "my-client" --workspace /path/to/workspace
uv run metis gateway --port 8422

# Re-run the heuristic evaluator over a window of trace events (evaluator.md §6.2)
uv run metis evaluate --db-path ~/.metis/metis.db --subject turn
uv run metis evaluate --db-path ~/.metis/metis.db --subject session --since 2026-05-01T00:00:00Z

# Real-API smoke tests
uv run python scripts/smoke.py --model haiku                # ~$0.015 / 2-turn run
uv run python scripts/smoke_cross_provider.py               # ~$0.007, mid-session provider switch
uv run python scripts/smoke_cache.py --model haiku          # < $0.05, asserts cache hits

# Savings benchmark suite (writes to benchmarks/.runs/)
uv run python scripts/benchmark.py                          # ~$0.30-1.00 full suite
uv run python scripts/benchmark.py --workload fix-a-bug-small   # ~$0.05-0.20 smoke
```

`.env` (gitignored) is the recommended place for API keys. Default SQLite path is `~/.metis/metis.db` (holds trace events + sessions/messages; override with `--db-path`).

## Repo layout

```
metis/
├── packages/
│   └── metis/                     # the single published package
│       ├── pyproject.toml
│       ├── src/metis/
│       │   ├── __init__.py
│       │   ├── core/              # library foundation; importable standalone
│       │   │   ├── canonical/     # Messages, ContentBlocks, ToolDefinition, AdapterCapabilities
│       │   │   ├── events/        # bus + envelope + payload catalog (24 types)
│       │   │   ├── trace/         # SQLite WAL writer + replay query
│       │   │   ├── tools/         # dispatcher + workspace API + confirmation + 5 file/shell builtins
│       │   │   ├── memory/        # MemoryStore (byte-budgeted MEMORY.md/USER.md) + 3 memory tools
│       │   │   ├── adapters/      # Anthropic / OpenAI / OpenRouter + retry + tool id map
│       │   │   ├── routing/       # registry + per-(provider, model) availability + chain + override parser + YAML rule policy
│       │   │   ├── pricing/       # PriceTable (Decimal) + overlay versioning
│       │   │   ├── analytics/     # AnalyticsStore.savings() + windows (backs /analytics/savings)
│       │   │   ├── patterns/      # PatternStore + structural fingerprint + K-NN aggregation (slot 4)
│       │   │   ├── eval/          # HeuristicJudge + Evaluator bus subscriber + BudgetTracker + `metis evaluate` CLI
│       │   │   ├── sessions/      # Session, InMemorySessionStore, SqliteSessionStore, SessionManager
│       │   │   └── skills/        # SkillStore + skill_load tool (agentskills.io-compatible)
│       │   ├── server/            # HTTP/WS agent surface; depends on metis.core
│       │   │   └── ...            # Starlette app + StreamingHub + token registry + TurnExecutor
│       │   ├── gateway/           # transparent OpenAI/Anthropic-shape HTTP proxy
│       │   │   └── ...            # Starlette app + Keystore + GatewayHarness + per-shape translators + issue-key
│       │   └── cli/               # CLI + Textual TUI + serve + gateway entry; provides the `metis` console-script
│       │       ├── chat.py main.py runtime.py serve.py models_display.py
│       │       └── tui/           # Textual TUI
│       └── tests/                 # mirrors src/metis/ layout (core/, server/, gateway/, cli/)
├── docs/                          # specs/, operations/, market-research/, KNOWN_ISSUES.md
├── benchmarks/                    # workload suite + RESULTS.md + .runs/ (gitignored)
├── scripts/                       # smoke.py, smoke_cross_provider.py, smoke_cache.py, benchmark.py (live-API harnesses)
├── tests_shared/                  # cross-member test helpers (scripted adapter, etc.)
├── conftest.py                    # workspace-root pytest config (adds tests_shared to sys.path)
├── pyproject.toml                 # workspace root: members, dev deps, ruff/pytest config
├── uv.lock
├── infra/                         # (empty placeholder for future deploy/CI/IaC artifacts)
└── README.md / AGENTS.md / CLAUDE.md
```

`CLAUDE.md` is a symlink to `AGENTS.md`. Specs live at `docs/specs/`; operational playbooks at `docs/operations/`; market research at `docs/market-research/`; carryover review findings at `docs/KNOWN_ISSUES.md`. The cost-optimization thesis, business model, sales toolkit, and operator-facing concierge / launch / billing runbooks all live in the private `metis-pro` repo.

## When in doubt

Ask before refactoring or expanding scope. The owner prefers a one-line clarifying question over a large speculative diff.
